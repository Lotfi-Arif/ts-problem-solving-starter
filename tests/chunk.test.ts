import { describe, it, expect } from 'vitest';
import { chunk_loop } from '@/problems/chunk/impl_baseline';

describe('chunk', () => {
  it('splits into equal parts and tail', () => {
    // TODO: add cases
    expect(true).toBe(true);
  });
});
