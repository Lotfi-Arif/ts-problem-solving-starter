import { RangeQuery } from './index';

export const prefix = (xs: ReadonlyArray<number>): number[] => {
  /* TODO */ return [];
};
export const makeRangeQuery = (pref: ReadonlyArray<number>): RangeQuery => {
  /* TODO */ return () => 0;
};
